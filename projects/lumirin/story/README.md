# LUMIRIN Brand Story

PDF의 브랜드 스토리를 반응형 웹 페이지로 재구성한 정적 사이트입니다.

## 실행

`index.html`을 브라우저에서 직접 열거나 이 폴더에서 로컬 서버를 실행하세요.

```bash
python -m http.server 8000
```

그 다음 `http://localhost:8000`으로 접속합니다.
